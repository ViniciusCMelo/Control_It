import React from "react";

import { StyleSheet } from "react-native";

let Shapes = StyleSheet.create({
	rounded: {
		display: "flex",
		justifyContent: "center",
		borderRadius: 50,
		width: 30,
		height: 30,
		backgroundColor: "red"
	},
});

export default Shapes;

